<%- model_class = EGCServerType -%>
<div class="page-header">
  <h1><%=t '.title', :default => EGCServerType %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th>ID</th>
      <th><%= model_class.human_attribute_name(:name) %></th>
      <th><%= model_class.human_attribute_name(:description) %></th>
      <th><%= model_class.human_attribute_name(:created_at) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @egc_server_types.each do |egc_server_type| %>
      <tr>
        <td><%= egc_server_type.id %></td>
        <td><%= egc_server_type.name %></td>
        <td><%= egc_server_type.description %></td>
        <td><%=l egc_server_type.created_at, format: :long %></td>
        <td>
          <%= link_to egc_server_type_path(egc_server_type), :class => 'btn btn-xs', :title => "#{ t('.show', :default => t('helpers.links.show')) }" do %>
            <%= glyph 'info-sign' %>
            <%= glyph 'pencil' %>
          <%- end -%>
          <%= link_to egc_server_type_path(egc_server_type), :method => :delete, :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, :class => 'btn btn-xs', :title => "#{ t('.destroy', :default => t('helpers.links.destroy')) }" do %>
            <%= glyph 'remove' %>
          <%- end -%>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_egc_server_type_path,
            :class => 'btn btn-primary' %>
