<%- model_class = QuoteRequest -%>
<div class="page-header">
  <h1><%=t '.title', :default => QuoteRequest %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th>ID</th>
      <th><%= model_class.human_attribute_name(:project) %></th>
      <th><%= model_class.human_attribute_name(:requested_at) %></th>
      <th><%= model_class.human_attribute_name(:description) %></th>
      <th><%= model_class.human_attribute_name(:created_at) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @quote_requests.each do |quote_request| %>
      <tr>
        <td><%= quote_request.id %></td>
        <td><%= quote_request.project %></td>
        <td><%= quote_request.requested_at %></td>
        <td><%= quote_request.description %></td>
        <td><%=l quote_request.created_at, format: :long %></td>
        <td>
          <%= link_to quote_request_path(quote_request), :class => 'btn btn-xs', :title => "#{ t('.show', :default => t('helpers.links.show')) }" do %>
            <%= glyph 'info-sign' %>
            <%= glyph 'pencil' %>
          <%- end -%>
          <%= link_to quote_request_path(quote_request), :method => :delete, :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, :class => 'btn btn-xs', :title => "#{ t('.destroy', :default => t('helpers.links.destroy')) }" do %>
            <%= glyph 'remove' %>
          <%- end -%>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_quote_request_path,
            :class => 'btn btn-primary' %>
