<%- model_class = QuoteRequestItemStorage -%>
<div class="page-header">
  <h1><%=t '.title', :default => QuoteRequestItemStorage %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th>ID</th>
      <th><%= model_class.human_attribute_name(:name) %></th>
      <th><%= model_class.human_attribute_name(:description) %></th>
      <th><%= model_class.human_attribute_name(:capacity) %></th>
      <th><%= model_class.human_attribute_name(:backup_size) %></th>
      <th><%= model_class.human_attribute_name(:created_at) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @quote_request_item_storages.each do |quote_request_item_storage| %>
      <tr>
        <td><%= quote_request_item_storage.id %></td>
        <td><%= quote_request_item_storage.name %></td>
        <td><%= quote_request_item_storage.description %></td>
        <td><%= quote_request_item_storage.capacity %></td>
        <td><%= quote_request_item_storage.backup_size %></td>
        <td><%=l quote_request_item_storage.created_at, format: :long %></td>
        <td>
          <%= link_to quote_request_item_storage_path(quote_request_item_storage), :class => 'btn btn-xs', :title => "#{ t('.show', :default => t('helpers.links.show')) }" do %>
            <%= glyph 'info-sign' %>
            <%= glyph 'pencil' %>
          <%- end -%>
          <%= link_to quote_request_item_storage_path(quote_request_item_storage), :method => :delete, :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, :class => 'btn btn-xs', :title => "#{ t('.destroy', :default => t('helpers.links.destroy')) }" do %>
            <%= glyph 'remove' %>
          <%- end -%>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_quote_request_item_storage_path,
            :class => 'btn btn-primary' %>
