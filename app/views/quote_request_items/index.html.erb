<%- model_class = QuoteRequestItem -%>
<div class="page-header">
  <h1><%=t '.title', :default => QuoteRequestItem %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th>ID</th>
      <th><%= model_class.human_attribute_name(:pieces) %></th>
      <th><%= model_class.human_attribute_name(:description) %></th>
      <th><%= model_class.human_attribute_name(:created_at) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @quote_request_items.each do |quote_request_item| %>
      <tr>
        <td><%= quote_request_item.id %></td>
        <td><%= quote_request_item.pieces %></td>
        <td><%= quote_request_item.description %></td>
        <td><%=l quote_request_item.created_at, format: :long %></td>
        <td>
          <%= link_to quote_request_item_path(quote_request_item), :class => 'btn btn-xs', :title => "#{ t('.show', :default => t('helpers.links.show')) }" do %>
            <%= glyph 'info-sign' %>
            <%= glyph 'pencil' %>
          <%- end -%>
          <%= link_to quote_request_item_path(quote_request_item), :method => :delete, :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, :class => 'btn btn-xs', :title => "#{ t('.destroy', :default => t('helpers.links.destroy')) }" do %>
            <%= glyph 'remove' %>
          <%- end -%>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_quote_request_item_path,
            :class => 'btn btn-primary' %>
