<%- model_class = QuoteRequestItemServer -%>
<div class="page-header">
  <h1><%=t '.title', :default => QuoteRequestItemServer %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th>ID</th>
      <th><%= model_class.human_attribute_name(:required_cpu) %></th>
      <th><%= model_class.human_attribute_name(:required_ram) %></th>
      <th><%= model_class.human_attribute_name(:created_at) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @quote_request_item_servers.each do |quote_request_item_server| %>
      <tr>
        <td><%= quote_request_item_server.id %></td>
        <td><%= quote_request_item_server.required_cpu %></td>
        <td><%= quote_request_item_server.required_ram %></td>
        <td><%=l quote_request_item_server.created_at, format: :long %></td>
        <td>
          <%= link_to quote_request_item_server_path(quote_request_item_server), :class => 'btn btn-xs', :title => "#{ t('.show', :default => t('helpers.links.show')) }" do %>
            <%= glyph 'info-sign' %>
            <%= glyph 'pencil' %>
          <%- end -%>
          <%= link_to quote_request_item_server_path(quote_request_item_server), :method => :delete, :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, :class => 'btn btn-xs', :title => "#{ t('.destroy', :default => t('helpers.links.destroy')) }" do %>
            <%= glyph 'remove' %>
          <%- end -%>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_quote_request_item_server_path,
            :class => 'btn btn-primary' %>
