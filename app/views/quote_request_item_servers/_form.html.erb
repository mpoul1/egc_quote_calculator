<%= form_for @quote_request_item_server, :html => { :class => 'form-horizontal form-model' } do |f| %>
  <div class="form-group">
    <%= f.label :required_cpu, :class => 'control-label col-md-2' %>
    <div class="col-md-10">
      <%= f.text_field :required_cpu, :class => 'text-field form-control' %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :required_ram, :class => 'control-label col-md-2' %>
    <div class="col-md-10">
      <%= f.number_field :required_ram, :class => 'text-field form-control' %>
    </div>
  </div>

  <div class="form-group">
    <div class='col-md-offset-2 col-md-10'>
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                  quote_request_item_servers_path, :class => 'btn btn-default' %>
    </div>
  </div>
<% end %>
